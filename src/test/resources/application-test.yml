spring:
  cloud:
    gateway:
      routes: []
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework.cloud.gateway: WARN
    org.springframework.security: WARN
